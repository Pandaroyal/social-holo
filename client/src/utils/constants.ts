export enum Status {
    idle = 'idle',
    loading = 'loading',
    succeeded = 'succeeded',
    failed = 'failed',
}

export enum UsersListType {
    FOLLOWERS = 'Followers',
    FOLLOWINGS = 'Followings',
    REQUESTS = 'Requests'
}